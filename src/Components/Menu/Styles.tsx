import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    margin: 8,
    flex: 1,
    justifyContent: 'space-around',
  },
  links: {
    marginBottom: 20,
    marginTop: 20,
  },
});
